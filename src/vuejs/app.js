const { createApp } = Vue;

createApp({
    //Data function
    data() {
        return {
            appTitle: 'Monster Hunter Guild Tasks',
            appSubtitle: 'Vue.js',

            //will auto update when UI changed
        }
    }
})